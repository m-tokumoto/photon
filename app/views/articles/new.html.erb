<div class="container">
	<div class="row">
		<div class="col"></div>
		<div class="col-mid-10">
			<%= form_for(@article) do |f| %>

				<label>タイトル</label>
				<%= f.text_field :title %>

				<%= f.cktext_area :text %>

				<%= f.submit "投稿" %>

			<% end %>
		</div>
		<div class="col"></div>
	</div>
</div>